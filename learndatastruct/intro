程序设计 = 数据结构+算法

数据结构：逻辑结构（数据间的关系） 物理结构
逻辑结构：集合（除同属于一个集合无其他关系）线性结构（一对一）树形结构（一对多）图型结构（多对多）
物理结构：如何把数据元素存储到计算机存储器中

存储结构：顺序存储（逻辑结构与物理结构一致） 链式存储

算法：高斯算法1+2+。。+100（输入输出有穷性确定性可行性）
（时间效率高，存储量低）

时间复杂度f(n) g(n) 执行次数=时间 O() O(1) O(n)for(n)  O(n^2)for(n for(n)) O(logn)  x^2=n x=log(2)n O(nlogn) 最多到n^3可接受 
空间复杂度


